#include <stdio.h>
/**
 * main- Entry point for program.
 * Return: 0 if no errors, return non-zero value if errors
 */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
